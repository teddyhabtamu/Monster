// Enums: a way to define a set of named constants

// Enums: allow us to define a collectionof related values, that can be used interchangeably

enum WeatherConditions {
  Sunny = 'sunny',
  Cloudy = 'cloudy',
  Rainy = 'Rainy',
  Snowy = 'snowy',
}

console.log(WeatherConditions.Snowy);

